export default function Header ({ tittle }){
    return(
        <div id="header">
            <nav>
                <header>
                    <h1>{tittle}</h1>
                </header>
            </nav>
        </div>
    )
}